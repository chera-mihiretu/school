# school

Host-based school platform: Next.js frontend, Node backend, Postgres.

- Local: `./deploy.sh` (Docker Compose on `http://e-school.et:3000`)
- Production: Railway on `https://e-school.et` — see `documents/railway.md`

Do not commit `.env` files. Copy `backend/.env.example` and `frontend/.env.example`.
